/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
} from "@solana/kit";
import { TOKEN_METADATA_PROGRAM_ADDRESS } from "../programs/index.js";
import type { ResolvedAccount } from "../shared/index.js";
import { getAccountMetaFactory } from "../shared/index.js";

export const REMOVE_CREATOR_VERIFICATION_DISCRIMINATOR = 28;

export function getRemoveCreatorVerificationDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(REMOVE_CREATOR_VERIFICATION_DISCRIMINATOR);
}

export type RemoveCreatorVerificationInstruction<
  TProgram extends string = typeof TOKEN_METADATA_PROGRAM_ADDRESS,
  TAccountMetadata extends string | AccountMeta = string,
  TAccountCreator extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMetadata extends string
        ? WritableAccount<TAccountMetadata>
        : TAccountMetadata,
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >;

export interface RemoveCreatorVerificationInstructionData {
  discriminator: number;
}

export interface RemoveCreatorVerificationInstructionDataArgs {}

export function getRemoveCreatorVerificationInstructionDataEncoder(): FixedSizeEncoder<RemoveCreatorVerificationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: REMOVE_CREATOR_VERIFICATION_DISCRIMINATOR,
    }),
  );
}

export function getRemoveCreatorVerificationInstructionDataDecoder(): FixedSizeDecoder<RemoveCreatorVerificationInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getRemoveCreatorVerificationInstructionDataCodec(): FixedSizeCodec<
  RemoveCreatorVerificationInstructionDataArgs,
  RemoveCreatorVerificationInstructionData
> {
  return combineCodec(
    getRemoveCreatorVerificationInstructionDataEncoder(),
    getRemoveCreatorVerificationInstructionDataDecoder(),
  );
}

export interface RemoveCreatorVerificationInput<
  TAccountMetadata extends string = string,
  TAccountCreator extends string = string,
> {
  /** Metadata (pda of ['metadata', program id, mint id]) */
  metadata: Address<TAccountMetadata>;
  /** Creator */
  creator: TransactionSigner<TAccountCreator>;
}

export function getRemoveCreatorVerificationInstruction<
  TAccountMetadata extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof TOKEN_METADATA_PROGRAM_ADDRESS,
>(
  input: RemoveCreatorVerificationInput<TAccountMetadata, TAccountCreator>,
  config?: { programAddress?: TProgramAddress },
): RemoveCreatorVerificationInstruction<
  TProgramAddress,
  TAccountMetadata,
  TAccountCreator
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TOKEN_METADATA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    metadata: { value: input.metadata ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  const instruction = {
    accounts: [
      getAccountMeta(accounts.metadata),
      getAccountMeta(accounts.creator),
    ],
    programAddress,
    data: getRemoveCreatorVerificationInstructionDataEncoder().encode({}),
  } as RemoveCreatorVerificationInstruction<
    TProgramAddress,
    TAccountMetadata,
    TAccountCreator
  >;

  return instruction;
}

export interface ParsedRemoveCreatorVerificationInstruction<
  TProgram extends string = typeof TOKEN_METADATA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    /** Metadata (pda of ['metadata', program id, mint id]) */
    metadata: TAccountMetas[0];
    /** Creator */
    creator: TAccountMetas[1];
  };
  data: RemoveCreatorVerificationInstructionData;
}

export function parseRemoveCreatorVerificationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveCreatorVerificationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      metadata: getNextAccount(),
      creator: getNextAccount(),
    },
    data: getRemoveCreatorVerificationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
